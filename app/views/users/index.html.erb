<% provide(:title, 'All users') %>
<h1>All users</h1>
<h2>soon to be admin access only</h2>

<%= will_paginate %>

<ul class="users">
    <!-- by calling render @users rails automatically looks for a partial called _user
         and renders it for each user in users-->
    <%= render @users %>
</ul>

<%= will_paginate %>
